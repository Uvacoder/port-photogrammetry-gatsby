schema: http://localhost:8000/___graphql
documents: "src/**/*.{ts,tsx}"
generates:
  ./src/types.d.ts:
    plugins:
      - typescript
      - typescript-operations
    config:
      avoidOptionals: true
      immutableTypes: true
